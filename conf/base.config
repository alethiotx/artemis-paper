process {

    cpus   = { 8 }
    memory = { 16.GB }
    time   = { 8.h  }

    withLabel:'cv|cv_range|targets|training|baselines|sabcs_consensus' {
        cpus   = { 16 }
        memory = { 64.GB * task.attempt }
    }

    errorStrategy = 'retry'
    maxRetries   = 3

}